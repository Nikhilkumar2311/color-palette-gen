const { randomPalette } = require('./randomPalette');
const { contrastCheck } = require('./contrastCheck');
const { generateTailwindColors } = require('./tailwindGen');

module.exports = {
  randomPalette,
  contrastCheck,
  generateTailwindColors
};
